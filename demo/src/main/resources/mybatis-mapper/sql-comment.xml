<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.common.comment.CommentMapper">

   <insert id="insertComment" useGeneratedKeys="true" keyProperty="commentNo">
    INSERT INTO Comment (
        post_no, user_id, content, reg_date
    ) VALUES (
        #{postNo}, #{userId}, #{content}, CURRENT_TIMESTAMP
    )
</insert>

    <select id="selectCommentList" resultType="com.example.demo.common.comment.Comment">
	    SELECT * FROM Comment 
	    WHERE post_no = #{targetId}  
    </select>

    <update id="deleteComment">
        UPDATE Comment SET is_deleted = 1 WHERE comment_no = #{commentNo}
    </update>

</mapper>