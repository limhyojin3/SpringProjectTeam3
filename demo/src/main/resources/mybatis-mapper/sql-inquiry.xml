<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.common.inquiry.InquiryMapper">

    <insert id="insertInquiry" useGeneratedKeys="true" keyProperty="inquiryNo">
        INSERT INTO Inquiry (
            user_id, title, content, reg_date
        ) VALUES (
            #{userId}, #{title}, #{content}, CURRENT_TIMESTAMP
        )
    </insert>

    <select id="selectInquiryList" resultType="com.example.demo.common.inquiry.Inquiry">
        SELECT * FROM Inquiry 
        WHERE user_id = #{userId} AND is_deleted = 0
        ORDER BY reg_date DESC
    </select>

    <update id="deleteInquiry">
        UPDATE Inquiry SET is_deleted = 1 WHERE inquiry_no = #{inquiryNo}
    </update>

</mapper>