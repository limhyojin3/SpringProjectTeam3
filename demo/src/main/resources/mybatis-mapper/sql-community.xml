<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.community.CommunityMapper">

    <insert id="insertPost" useGeneratedKeys="true" keyProperty="postNo">
        INSERT INTO Community_Post (
            user_id, category, title, content, reg_date
        ) VALUES (
            #{userId}, #{category}, #{title}, #{content}, CURRENT_TIMESTAMP
        )
    </insert>

    <select id="selectPostList" resultType="com.example.demo.community.Community">
        SELECT * FROM Community_Post 
        WHERE is_deleted = 0 
        ORDER BY reg_date DESC
    </select>

    <select id="selectPostById" resultType="com.example.demo.community.Community">
        SELECT * FROM Community_Post WHERE post_no = #{postNo}
    </select>

    <select id="checkPostLike" resultType="int">
        SELECT COUNT(*) FROM Post_Like 
        WHERE post_no = #{postNo} AND user_id = #{userId}
    </select>

    <insert id="insertPostLike">
        INSERT INTO Post_Like (post_no, user_id, reg_date)
        VALUES (#{postNo}, #{userId}, CURRENT_TIMESTAMP)
    </insert>

    <delete id="deletePostLike">
        DELETE FROM Post_Like 
        WHERE post_no = #{postNo} AND user_id = #{userId}
    </delete>

    <update id="updatePostLikeCount">
        UPDATE Community_Post SET like_cnt = like_cnt + #{amount} 
        WHERE post_no = #{postNo}
    </update>

</mapper>