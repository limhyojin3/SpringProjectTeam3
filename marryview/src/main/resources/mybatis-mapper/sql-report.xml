<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<<<<<<< HEAD
<mapper namespace="com.example.demo.community_review.mapper.ReportMapper">
	
	<!--신고 입력-->
    <insert id="insertReport" useGeneratedKeys="true" keyProperty="reportNo">
        INSERT INTO Report (
            reporter_id, target_type, target_id, target_user_id, 
            report_title, report_content, target_title, reg_date 
        ) VALUES (
            #{reporterId}, #{targetType}, #{targetId}, #{targetUserId}, 
            #{reportTitle}, #{reportContent}, #{targetTitle}, CURRENT_TIMESTAMP
        )
    </insert>
    
    <!--중복 신고 체크-->
    <select id="checkDuplicateReport" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM Report
        WHERE reporter_id = #{reporterId}
          AND target_type = #{targetType}
          AND target_id = #{targetId} 
    </select>

	<!-- 내 신고 내역 -->
    <select id="selectMyReports" resultType="com.example.demo.community_review.model.Report">
=======
<mapper namespace="com.example.marryview.community_review.mapper.ReportMapper">
	
	<!--신고 입력-->
    <insert id="insertReport" useGeneratedKeys="true" keyProperty="reportNo">
        INSERT INTO Report (
            reporter_id, target_type, target_id, target_user_id, 
            report_title, report_content, target_title, reg_date 
        ) VALUES (
            #{reporterId}, #{targetType}, #{targetId}, #{targetUserId}, 
            #{reportTitle}, #{reportContent}, #{targetTitle}, CURRENT_TIMESTAMP
        )
    </insert>
    
    <!--중복 신고 체크-->
    <select id="checkDuplicateReport" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM Report
        WHERE reporter_id = #{reporterId}
          AND target_type = #{targetType}
          AND target_id = #{targetId} 
    </select>

	<!-- 내 신고 내역 -->
    <select id="selectMyReports" resultType="com.example.marryview.community_review.model.Report">
>>>>>>> branch 'dev_' of https://github.com/limhyojin3/SpringProjectTeam3.git
        SELECT 
            report_no AS reportNo,
            reporter_id AS reporterId,
            target_type AS targetType,
            target_id AS targetId,
            target_user_id AS targetUserId,
            report_title AS reportTitle,
            report_content AS reportContent,
            answer_status AS answerStatus,
            answer_content AS answerContent,
            action_status AS actionStatus,
            reg_date AS regDate,
            target_title AS targetTitle
        FROM Report 
        WHERE reporter_id = #{reporterId}
        <if test="searchType != null and searchType != ''">
            AND target_type = #{searchType}
        </if>
        <if test="searchStatus != null and searchStatus != ''">
            AND answer_status = #{searchStatus}
        </if>
        ORDER BY reg_date DESC
        LIMIT #{startIndex}, #{pageSize}
    </select>

    <select id="selectReportCount" resultType="int">
        SELECT COUNT(*)
        FROM Report
        WHERE reporter_id = #{reporterId}
        <if test="searchType != null and searchType != ''">
            AND target_type = #{searchType}
        </if>
        <if test="searchStatus != null and searchStatus != ''">
            AND answer_status = #{searchStatus}
        </if>
    </select>

</mapper>